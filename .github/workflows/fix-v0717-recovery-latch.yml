name: Fix Runner v0.7.17 recovery latch

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: write

concurrency:
  group: fix-runner-v0717-recovery-latch-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  fix:
    if: ${{ github.head_ref == 'agent/v0717-eye-test-feet-gait-batch25' && github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v0717-eye-test-feet-gait-batch25
          fetch-depth: 0

      - name: Cache exact recovery-latch findings
        run: python tools/fix_v0717_recovery_latch.py cache

      - name: Commit finding before correction
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add missioncache.md
          git diff --cached --check
          git commit -m 'Cache Runner v0.7.17 recovery latch findings'
          git push origin HEAD:agent/v0717-eye-test-feet-gait-batch25

      - name: Correct shared recovery and squat-test paths
        run: python tools/fix_v0717_recovery_latch.py implement

      - name: Commit source correction
        shell: bash
        run: |
          set -euo pipefail
          rm tools/fix_v0717_recovery_latch.py
          git add -A
          git diff --cached --check
          git commit -m 'Correct Runner v0.7.17 recovery latch and settling'
          git push origin HEAD:agent/v0717-eye-test-feet-gait-batch25
