# EpochRunner v0.7.2 validation history

The runtime screenshots reopened the v0.7.1 quality claims and the correction was validated iteratively rather than hiding failed attempts.

- Initial push workflow `30728242767`: failed before compilation.
- Pull-request validation `30731942858`: found the course-schedule edit routed to `simulation.cpp` instead of `simulation.hpp`.
- Pull-request validation `30731997915`: source built successfully; an obsolete test still allowed duck imitation without clearing a low bar.
- Pull-request validation `30732079896`: heel/toe support nodes caused the old support-count heuristic to skip humanoid joint coordination.
- Pull-request validation `30732150992`: the topology repair did not apply because its temporary exact-text matcher depended on whitespace.
- Pull-request validation `30732207865`: topology replacement reached GCC, which rejected misleading generated indentation under `-Werror`.
- Pull-request validation `30732271858`: coordination built and ran; the initial low-bar command was too weak because assistance used an obstacle weight cached only after a physics step.
- Pull-request validation `30732352085`: immediate low-bar guidance passed, then balance exposed a short floppy ankle-to-foot link.
- Pull-request validation `30732431844`: increased separation alone still allowed the foot plate to rotate under the ankle.
- Pull-request validation `30732573644`: braced feet passed; historical stance evidence still allowed a collapsed current display frame.
- Pull-request validation `30732668170`: the first current-frame gate was too strict for a legitimate crouched posture.
- Pull-request validation `30732728412`: head-to-pelvis distance still rejected a valid balanced crouch.
- Pull-request validation `30732783454`: **passed**. GCC 14 C++23 build, core regression tests, concurrency benchmark, and runtime pipeline all succeeded using direct pelvis-to-torso and torso-to-head integrity checks.
- Pull-request validation `30732862342`: an evidence-only trigger checked a stale pre-cleanup Windows head and correctly refused to build it.
- Pull-request validation `30732908362`: Linux passed and published the unique clean posture implementation; its Windows job still referenced the earlier event head and correctly refused it.

Current clean correction source before this evidence-only retry: `38134978a08ef59d793fbc82c45bbe4090bebacb`.
