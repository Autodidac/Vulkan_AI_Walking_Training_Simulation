# EpochRunner v0.7.2 Linux materialization history

- Initial push workflow `30728242767`: failed before compilation.
- Pull-request validation `30731942858`: found the course-schedule edit routed to `simulation.cpp` instead of `simulation.hpp`.
- Pull-request validation `30731997915`: source built successfully; the remaining failure was an obsolete test that allowed duck imitation without clearing a low bar.
- The validation gate now requires obstacle-conditioned duck-and-clear evidence consistently.
