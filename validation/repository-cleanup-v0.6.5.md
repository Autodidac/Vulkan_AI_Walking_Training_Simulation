# Repository cleanup after Runner v0.6.5

- Open pull requests: `0`
- Remaining branches:
  - `main`
- Deleted completed temporary branches:
  - `feature/mission-cache-completion`
  - `feature/self-imitation-mission-cache`
  - `feature/v070-runtime-pipeline`
  - `diagnostics/action-status`
- Removed stale v0.6.1 release workflow from the former v0.7 branch.
- Removed obsolete `tools/integrate_v061.py` from the former v0.7 branch.
- Removed temporary validation and cleanup workflows after they ran.
- Historical closed pull requests remain as GitHub audit history; GitHub does not provide repository owners a way to delete pull-request records.
- Pull requests `#19` and `#20` were accidentally created during final verification and immediately closed without merge. They remain only in closed history.
- Current repository state is one branch (`main`) and zero open pull requests.
