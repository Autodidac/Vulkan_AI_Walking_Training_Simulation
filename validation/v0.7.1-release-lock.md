# EpochRunner v0.7.1 release lock

The initial parent-reaction release gate was superseded before publication because its regression test exercised the startup control ramp rather than the motor solver directly.

Only the refined gate may publish v0.7.1. It must validate direct parent/pivot reaction, driven-side dominance, and center-of-mass preservation, then remove this lock file in the tested source commit.
